# This config file contains the crop configuration for an image.
# The first lines, starting with a comment char '#' will be ignored and can be used for comments
# The following lines need to stay in the correct order
# All lines can include comments after the inputs. These comments have to start with the comment char '#'
#
# The following lines are the config lines:
Dwarf_Sprite_Sheet1.2v.png# inputImageFile
318                       # width
32                        # height
dwarf_right_stay dwarf_right_run dwarf_right_attack_jump dwarf_right_attack dwarf_right_spin dwarf_right_jump dwarf_right_hit dwarf_right_fall dwarf_left_stay dwarf_left_run dwarf_left_attack_jump dwarf_left_attack dwarf_left_spin dwarf_left_jump dwarf_left_hit dwarf_left_fall # outputNamesPerLine (delimiter is a space character ' ')
dwarfs                    # outputDirName
n                         # continuousIndices (y|n)
0                         # xOffset
0                         # yOffset
n                         # xOffsetStartsBeforeImage (y|n)
n                         # yOffsetStartsBeforeImage (y|n)
-1                        # numImages (-1 for 'count them yourself!')